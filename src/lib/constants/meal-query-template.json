{
  "prompt": "Create $NUM_RESULTS different daily meal plans comprised of $MEAL_TYPES that serves $NUM_SERVINGS persons for each meal, with a total daily calorie count as close to $CALORIES per person.",
  "variables": {
    "ingredients": {
      "symbol": "$INGREDIENTS",
      "type": "array",
      "replace": "Some of the meals should use one or more of these ingredients: $ARRAY"
    },
    "calories": {
      "symbol": "$CALORIES",
      "type": "number"
    },
    "mealTypes": {
      "symbol": "$MEAL_TYPES",
      "type": "array"
    },
    "numResults": {
      "symbol": "$NUM_RESULTS",
      "type": "number"
    }
  }
}
